using UnityEngine;

namespace Script.Game.Base
{
    public class PlayerInput : MonoBehaviour
    {
        
        private void Update()
        {
            if (Input.GetKeyDown(KeyCode.A))  //进攻
            {
                   
            }
            else if (Input.GetKeyDown(KeyCode.R))   //撤退
            {
                
            }
        }
    }
}